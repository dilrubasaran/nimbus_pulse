import 'package:flutter/material.dart';

// Renk Tanımlamaları
const Color primaryColor = Colors.blue; // Birincil renk
const Color secondaryColor = Colors.grey; // İkincil renk
const Color backgroundColor = Colors.white; // Arkaplan rengi
// Renkler
const Color bgPrimaryColor = Color(0xFFEFF8FF);
const Color primaryTextColor = Color(0xFF177EC6);
const Color secondaryTextColor = Color(0xFF8CC7F4);

// Font Ailesi
const String fontNunitoSans = "NunitoSans";

// Font Tanımlamaları
const String fontFamily = "Nunito Sans"; // Font ailesi

// Sabit Boyutlar
const double defaultPadding = 16.0; // Varsayılan padding
